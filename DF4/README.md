# DF4 — AXI Slave & GPIO

.\build.bat            - create project
.\build.bat -all       - build all stages
.\build.bat -syn       - run synthesis only
.\build.bat -impl -bit - run implementation and generate bitstream
.\build.bat -xsa       - generate XSA file
